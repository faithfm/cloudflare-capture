# The ONE per-repo setting: which Cloudflare account this repo captures.
# Everything account-specific derives from it, including the API-token
# Keychain item name: cloudflare-api-token-<ACCOUNT_ID>.
# Account: (describe it — e.g. "personal account, you@example.com")
ACCOUNT_ID=__ACCOUNT_ID__
